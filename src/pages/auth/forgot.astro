---
import Layout from "../../layouts/Layout.astro";
import { ForgotPasswordFormWithProvider } from "../../components/auth/ForgotPasswordFormWithProvider";

// This page is public (no auth required)
// ForgotPasswordFormWithProvider includes AuthPageControls (dark mode + language toggles)
---

<Layout title="Forgot Password - SavorAI" showHeader={false}>
  <main class="container mx-auto px-4 py-16 min-h-screen flex items-center justify-center">
    <div class="w-full max-w-md">
      <ForgotPasswordFormWithProvider client:only="react" />
    </div>
  </main>
</Layout>
